---
concurrency:
  cancel-in-progress: true
  group: ${{ github.workflow }}-${{ github.ref }}
jobs:
  e2e-tests:
    needs: integration-tests
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.13"
      - name: Install dependencies
        run: pip install pytest -e .
      - name: E2E tests
        run: python3 -m pytest test/e2e/ --verbose --pythonwarnings=error
  integration-tests:
    needs: unit-tests
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.13"
      - name: Install dependencies
        run: pip install pytest pytest-cov -e .
      - name: Integration tests
        run: |
          python3 -m pytest test/integration/ \
            --verbose --pythonwarnings=error \
            --cov=assert_no_linter_config_files \
            --cov-fail-under=100
  release:
    if: github.event_name == 'push' && github.ref == 'refs/heads/main'
    needs: e2e-tests
    permissions:
      contents: write
      id-token: write
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.13"
      - id: version
        name: Generate version from UTC time
        run: echo "version=$(date -u +%Y%m%d%H%M%S)" >> $GITHUB_OUTPUT
      - name: Create tag
        run: git tag "v${{ steps.version.outputs.version }}"
      - name: Install build tools
        run: pip install build
      - name: Build package
        run: python -m build
      - name: Push tag
        run: git push origin "v${{ steps.version.outputs.version }}"
      - name: Publish to PyPI
        uses: pypa/gh-action-pypi-publish@release/v1
      - env:
          GH_TOKEN: ${{ github.token }}
        name: Create GitHub Release
        run: |
          gh release create "v${{ steps.version.outputs.version }}" \
            --generate-notes
      - name: Update latest tag
        run: |
          git tag -f latest
          git push origin latest --force
  static-analysis:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.13"
      - name: Set up Node
        uses: actions/setup-node@v4
        with:
          node-version: "20"
      - name: Install dependencies
        run: |
          pip install yamllint pylint pytest mypy assert-no-inline-directives -e .
          npm install -g markdownlint-cli jscpd
      - name: Assert no linter config files
        run: |
          assert-no-linter-config-files --verbose \
            --linters jscpd,mypy,pylint,pytest,yamllint .
      - name: Assert no inline lint disables
        run: |
          assert-no-inline-directives --verbose \
            --tools mypy,pylint,yamllint \
            '**/*.py' '**/*.toml' '**/*.yml'
      - name: yamllint
        run: |
          EMPTY_LINES='empty-lines: {max: 0, max-start: 0, max-end: 1}'
          KEY_ORDERING='key-ordering: enable'
          TRUTHY='truthy: {allowed-values: ["true", "false", "on"]}'
          RULES="$EMPTY_LINES, $KEY_ORDERING, $TRUTHY"
          yamllint --strict --format github \
            --config-data "{extends: default, rules: {$RULES}}" \
            .github/workflows/*.yml
      - name: markdownlint
        run: markdownlint '*.md'
      - name: pylint (src)
        run: |
          python3 -m pylint src/ --fail-on=C,R,W --fail-under=10.0 \
            --disable=import-outside-toplevel,too-few-public-methods
      - name: pylint (test)
        run: |
          python3 -m pylint test/ --fail-on=C,R,W --fail-under=10.0 \
            --disable=import-outside-toplevel,too-few-public-methods
      - name: mypy (src)
        run: python3 -m mypy --strict src/
      - name: mypy (test)
        run: python3 -m mypy test/
      - name: jscpd (src)
        run: jscpd --pattern "**/*.py" --threshold 0 --reporters console src/
      - name: jscpd (test)
        run: jscpd --pattern "**/*.py" --threshold 0 --reporters console test/
  unit-tests:
    needs: static-analysis
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.13"
      - name: Install dependencies
        run: pip install pytest pytest-cov -e .
      - name: Unit tests
        run: |
          python3 -m pytest test/unit/ \
            --verbose --pythonwarnings=error \
            --cov=assert_no_linter_config_files \
            --cov-fail-under=100
name: CI
on:
  pull_request:
    branches:
      - main
    paths:
      - "*.md"
      - ".github/workflows/*.yml"
      - "pyproject.toml"
      - "src/**"
      - "test/**"
  push:
    branches:
      - main
    paths:
      - "*.md"
      - ".github/workflows/*.yml"
      - "pyproject.toml"
      - "src/**"
      - "test/**"
  workflow_dispatch:
